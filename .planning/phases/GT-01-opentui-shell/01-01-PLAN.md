---
phase: 01-opentui-shell
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/focus.ts, src/focus.test.ts, src/index.tsx]
autonomous: true
requirements: [TUI-01, TUI-05]
user_setup: []
must_haves:
  truths:
    - "TUI-01: src/index.tsx renders exactly three bordered panes titled What, Who, and Why at startup."
    - "TUI-05: src/focus.ts exports a deterministic focus cycle for what -> who -> why -> what."
    - "D-01: Pane titles use What, Who, and Why."
    - "D-02: Pane bodies use No file under examination, No suspects identified, and No theory formed."
    - "D-03: No active perspective text is rendered in Phase 1."
    - "D-04: No counts or idle/status hints are rendered in Phase 1."
    - "D-06: The focused pane is visually distinguished with minimal border styling."
  artifacts:
    - "src/focus.ts"
    - "src/focus.test.ts"
    - "src/index.tsx"
  key_links:
    - "src/index.tsx imports focus constants/helpers from src/focus.ts."
    - "src/focus.test.ts verifies the cycle exported by src/focus.ts."
---

<objective>
Build the first visible OpenTUI shell with three stable panes and a typed focus model.

Purpose: Prove the terminal UI scaffold can render the core What/Who/Why forensic frame before git data or AI analysis exists.
Output: A typed focus helper, a Bun test for focus order, and a replaced root OpenTUI view in `src/index.tsx`.
</objective>

<execution_context>
@/Users/jank/dev/personal/git-therapy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jank/dev/personal/git-therapy/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/GT-01-opentui-shell/01-SPEC.md
@.planning/phases/GT-01-opentui-shell/01-CONTEXT.md
@.planning/phases/GT-01-opentui-shell/01-RESEARCH.md
@.planning/phases/GT-01-opentui-shell/01-VALIDATION.md
@.planning/phases/GT-01-opentui-shell/01-UI-SPEC.md
@.planning/phases/GT-01-opentui-shell/01-PATTERNS.md
@src/index.tsx
@package.json
@tsconfig.json
</context>

<user_story>
As a demo viewer, I want the app to open into three clearly labeled terminal panes, so that the forensic report structure is visible before evidence is wired in.
</user_story>

<tasks>

<task type="tdd:red">
  <name>Task 1: Add failing focus-cycle tests</name>
  <files>src/focus.test.ts</files>
  <read_first>.planning/phases/GT-01-opentui-shell/01-SPEC.md, .planning/phases/GT-01-opentui-shell/01-CONTEXT.md, .planning/phases/GT-01-opentui-shell/01-RESEARCH.md, package.json, tsconfig.json</read_first>
  <action>Create `src/focus.test.ts` using Bun's built-in `bun:test` API. Import `describe`, `expect`, and `test` from `bun:test`. Import `getNextPaneId`, `INITIAL_FOCUSED_PANE_ID`, and `PANE_IDS` from `./focus`. Add tests asserting `INITIAL_FOCUSED_PANE_ID` is `what`, `PANE_IDS` equals `["what", "who", "why"]`, and `getNextPaneId` returns `who` for `what`, `why` for `who`, and `what` for `why`. Run the test before `src/focus.ts` exists and confirm RED fails because the module or exports are missing.</action>
  <verify>bun test src/focus.test.ts fails before implementation</verify>
  <acceptance_criteria>
    - `src/focus.test.ts` contains `from "bun:test"`.
    - `src/focus.test.ts` imports `getNextPaneId`, `INITIAL_FOCUSED_PANE_ID`, and `PANE_IDS` from `./focus`.
    - `src/focus.test.ts` contains the exact strings `"what"`, `"who"`, and `"why"`.
    - `bun test src/focus.test.ts` exits non-zero before `src/focus.ts` is implemented.
  </acceptance_criteria>
  <done>RED test exists and fails for the expected missing focus implementation.</done>
</task>

<task type="tdd:green">
  <name>Task 2: Implement typed pane focus helpers</name>
  <files>src/focus.ts</files>
  <read_first>src/focus.test.ts, .planning/phases/GT-01-opentui-shell/01-PATTERNS.md, tsconfig.json</read_first>
  <action>Create `src/focus.ts`. Export `PANE_IDS` as a readonly tuple `["what", "who", "why"]`. Export `type PaneId = (typeof PANE_IDS)[number]`. Export `INITIAL_FOCUSED_PANE_ID` with value `"what"`. Export `getNextPaneId(current: PaneId): PaneId`, implemented so the order is `what -> who -> why -> what`. Keep the helper pure and free of OpenTUI, React, git, AI, process, or terminal APIs.</action>
  <verify>bun test src/focus.test.ts && bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/focus.ts` contains `export const PANE_IDS`.
    - `src/focus.ts` contains `export type PaneId`.
    - `src/focus.ts` contains `export const INITIAL_FOCUSED_PANE_ID = "what"`.
    - `src/focus.ts` contains `export function getNextPaneId(current: PaneId): PaneId`.
    - `bun test src/focus.test.ts` exits 0.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Focus helper exists, is typed, and passes the RED test.</done>
</task>

<task type="tdd:refactor">
  <name>Task 3: Replace starter view with three focusable panes</name>
  <files>src/index.tsx</files>
  <read_first>src/index.tsx, src/focus.ts, .planning/phases/GT-01-opentui-shell/01-UI-SPEC.md, .planning/phases/GT-01-opentui-shell/01-PATTERNS.md</read_first>
  <action>Replace the starter `ascii-font` placeholder with the Phase 1 shell. Import `TextAttributes` from `@opentui/core`, `createRoot` from `@opentui/react`, `useState` from `react`, and `INITIAL_FOCUSED_PANE_ID` plus `PaneId` from `./focus`. Define pane data for three panes: id `what`, title `What`, body `No file under examination`; id `who`, title `Who`, body `No suspects identified`; id `why`, title `Why`, body `No theory formed`. In `App`, initialize `const [focusedPane] = useState<PaneId>(INITIAL_FOCUSED_PANE_ID)` so keyboard wiring can update it in Plan 01-02. Render one horizontal root box with three child `<box>` panes using `title`, `border`, `focusable`, `focused={focusedPane === pane.id}`, neutral border color, focused border color `#A6E22E`, one-cell padding, and approximate width allocation 60/20/20. Use dim body text. Do not render active perspective text, counts, status hints, footer, git placeholders, author lists, analysis state, model calls, or the starter `OpenTUI` ascii-font.</action>
  <verify>bun test src/focus.test.ts && bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/index.tsx` contains `title="What"` or `title={pane.title}` with pane data containing `title: "What"`.
    - `src/index.tsx` contains `title: "Who"` and `title: "Why"`.
    - `src/index.tsx` contains `No file under examination`, `No suspects identified`, and `No theory formed`.
    - `src/index.tsx` contains `focused={focusedPane === pane.id}`.
    - `src/index.tsx` does not contain `ascii-font`.
    - `src/index.tsx` does not contain `0 lines`, `0 authors`, `idle`, `Mental`, `Skill`, `Context`, or `Hidden`.
    - `bun test src/focus.test.ts` exits 0.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The terminal app renders the Phase 1 three-pane shell with initial focus state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Data crossing | Phase 1 handling |
|----------|---------------|------------------|
| Terminal to React state | Focus state only | Pane IDs are closed over the typed `PaneId` union. |
| Source code to terminal renderer | Static pane labels and body copy | Copy is hard-coded from UI-SPEC and does not include user input. |

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-01-01 | Tampering | Focus state | mitigated | `getNextPaneId` accepts only `PaneId`; tests prove invalid order is not introduced. |
| TM-01-02 | Information disclosure | Pane copy | not_applicable | Phase 1 renders no git data, no author data, no file paths, and no model output. |
| TM-01-03 | Elevation of privilege | Layout shell | not_applicable | Plan 01-01 adds no shell execution, file reads, network calls, or process control. |

Blocker threshold: high. No high-severity threats are expected for this local static terminal shell.
</threat_model>

## Artifacts this phase produces

- `src/focus.ts`
- `src/focus.test.ts`
- `PaneId`
- `PANE_IDS`
- `INITIAL_FOCUSED_PANE_ID`
- `getNextPaneId(current: PaneId): PaneId`
- Three pane definitions in `src/index.tsx`: `what`, `who`, `why`

<verification>
Before declaring plan complete:
- [ ] `bun test src/focus.test.ts` exits 0.
- [ ] `bunx tsc --noEmit` exits 0.
- [ ] `src/index.tsx` contains the pane titles `What`, `Who`, and `Why`.
- [ ] `src/index.tsx` contains the pane body copy `No file under examination`, `No suspects identified`, and `No theory formed`.
- [ ] `src/index.tsx` contains no starter `ascii-font` placeholder.
- [ ] `src/index.tsx` contains no footer, status line, perspective label, author list, git loading, or analysis state.
</verification>

<success_criteria>
- RED/GREEN/REFACTOR commits exist for the focus helper if execute-phase enforces TDD commits.
- `TUI-01` is satisfied by three always-visible panes.
- `TUI-05` has a typed focus model ready for keyboard handling in Plan 01-02.
- Decisions `D-01`, `D-02`, `D-03`, `D-04`, and `D-06` are represented in source and verified by source assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/GT-01-opentui-shell/01-01-SUMMARY.md`.
</output>
