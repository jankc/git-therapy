---
phase: 01-opentui-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/index.tsx]
autonomous: false
requirements: [CLI-03, TUI-05]
user_setup:
  - "Manual terminal verification is required because Tab focus and q exit are interactive OpenTUI behavior."
must_haves:
  truths:
    - "CLI-03: pressing q exits the TUI process cleanly."
    - "TUI-05: pressing Tab cycles focused pane from What to Who to Why and back to What."
    - "D-05: implementation stays minimal and preserves core functionality."
    - "D-07: hotkeys 1, 2, 3, and 4 are registered as inert handlers."
    - "D-08: Phase 1 still renders only the three panes, with no keymap footer or status line."
  artifacts:
    - "src/index.tsx"
  key_links:
    - "src/index.tsx imports `useKeyboard` from `@opentui/react`."
    - "src/index.tsx uses `getNextPaneId` from `src/focus.ts` for Tab cycling."
---

<objective>
Wire the Phase 1 keyboard behavior for focus cycling, quit, and inert perspective hotkeys.

Purpose: Make the shell demonstrable with keyboard interaction while keeping later git, author, and AI behavior out of scope.
Output: `src/index.tsx` uses OpenTUI keyboard input for Tab, q, and explicit no-op hotkeys 1 through 4.
</objective>

<execution_context>
@/Users/jank/dev/personal/git-therapy/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jank/dev/personal/git-therapy/.codex/get-shit-done/templates/summary.md
@/Users/jank/dev/personal/git-therapy/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/GT-01-opentui-shell/01-SPEC.md
@.planning/phases/GT-01-opentui-shell/01-CONTEXT.md
@.planning/phases/GT-01-opentui-shell/01-RESEARCH.md
@.planning/phases/GT-01-opentui-shell/01-VALIDATION.md
@.planning/phases/GT-01-opentui-shell/01-UI-SPEC.md
@.planning/phases/GT-01-opentui-shell/01-PATTERNS.md
@.planning/phases/GT-01-opentui-shell/01-01-SUMMARY.md
@src/index.tsx
@src/focus.ts
</context>

<user_story>
As a demo viewer, I want the visible shell to respond to Tab and q, so that the first terminal artifact feels like a real app instead of static output.
</user_story>

<tasks>

<task type="auto">
  <name>Task 1: Wire Tab focus cycling through OpenTUI keyboard input</name>
  <files>src/index.tsx</files>
  <read_first>src/index.tsx, src/focus.ts, .planning/phases/GT-01-opentui-shell/01-UI-SPEC.md, .planning/phases/GT-01-opentui-shell/01-PATTERNS.md</read_first>
  <action>Import `useKeyboard` from `@opentui/react` alongside `createRoot`. Import `getNextPaneId` from `./focus`. Change the Plan 01-01 focus state declaration to `[focusedPane, setFocusedPane]`. Add a `useKeyboard` handler in `App` that checks `key.name === "tab"` and calls `setFocusedPane((current) => getNextPaneId(current))`. The handler must not create or remove panes, must not add a footer or keymap text, and must not introduce author, git, model, or perspective state.</action>
  <verify>bun test src/focus.test.ts && bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/index.tsx` imports `useKeyboard` from `@opentui/react`.
    - `src/index.tsx` imports `getNextPaneId` from `./focus`.
    - `src/index.tsx` contains `key.name === "tab"`.
    - `src/index.tsx` contains `setFocusedPane((current) => getNextPaneId(current))`.
    - `src/index.tsx` still contains exactly the pane titles `What`, `Who`, and `Why`.
    - `bun test src/focus.test.ts` exits 0.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Tab changes the focused pane using the typed focus helper.</done>
</task>

<task type="auto">
  <name>Task 2: Add q quit behavior</name>
  <files>src/index.tsx</files>
  <read_first>src/index.tsx, .planning/phases/GT-01-opentui-shell/01-SPEC.md, .planning/phases/GT-01-opentui-shell/01-RESEARCH.md</read_first>
  <action>In the same `useKeyboard` handler, add a branch for `key.name === "q"` that calls `process.exit(0)`. Keep this as the only process-control behavior in Phase 1. Do not add Escape handling unless the source already has it, do not run shell commands, and do not add cleanup abstractions beyond the existing top-level `createCliRenderer()` scaffold.</action>
  <verify>bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/index.tsx` contains `key.name === "q"`.
    - `src/index.tsx` contains `process.exit(0)`.
    - `src/index.tsx` does not contain `exec(`, `spawn(`, `git blame`, `fetch(`, `streamText`, or `generateObject`.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Pressing q is wired to clean process exit.</done>
</task>

<task type="auto">
  <name>Task 3: Register perspective hotkeys as inert no-ops</name>
  <files>src/index.tsx</files>
  <read_first>src/index.tsx, .planning/phases/GT-01-opentui-shell/01-CONTEXT.md, .planning/phases/GT-01-opentui-shell/01-UI-SPEC.md</read_first>
  <action>Add a local constant `PERSPECTIVE_HOTKEYS = new Set(["1", "2", "3", "4"])` near the pane definitions. In the `useKeyboard` handler, add a branch that checks `PERSPECTIVE_HOTKEYS.has(key.name)` and immediately returns. Do not store active perspective state, do not render active perspective text, do not call a registry, do not call git, do not call AI providers, and do not add visible UI for these keys.</action>
  <verify>bunx tsc --noEmit</verify>
  <acceptance_criteria>
    - `src/index.tsx` contains `PERSPECTIVE_HOTKEYS`.
    - `src/index.tsx` contains `"1"`, `"2"`, `"3"`, and `"4"` inside the hotkey registration.
    - `src/index.tsx` contains `PERSPECTIVE_HOTKEYS.has(key.name)`.
    - `src/index.tsx` does not contain `activePerspective`, `setActivePerspective`, `Mental`, `Skill`, `Context`, or `Hidden`.
    - `src/index.tsx` does not contain `git blame`, `Anthropic`, `OpenAI`, `streamText`, or `generateObject`.
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Perspective hotkeys are auditable and inert.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>OpenTUI Phase 1 shell with three panes, Tab focus movement, q exit, and inert 1-4 hotkeys.</what-built>
  <how-to-verify>Run `bun dev` in a real terminal. Verify startup shows the What, Who, and Why panes simultaneously. Press Tab three times and confirm focus moves What -> Who -> Why -> What. Press 1, 2, 3, and 4 and confirm there is no visible change and no crash. Press q and confirm the process exits without an unhandled error.</how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Data crossing | Phase 1 handling |
|----------|---------------|------------------|
| Terminal keyboard input to app state | Key names `tab`, `q`, `1`, `2`, `3`, `4` | Only closed-set branches are handled. |
| App process to host shell | `process.exit(0)` for q | Exit is explicit, local, and not parameterized by user input. |

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-01-04 | Denial of service | Keyboard handler | accepted_low | Pressing q exits by design for CLI-03; no destructive work or background process is started. |
| TM-01-05 | Elevation of privilege | Hotkey handling | mitigated | Hotkeys 1-4 return without shell execution, file IO, git calls, model calls, or dynamic imports. |
| TM-01-06 | Information disclosure | Keyboard handling | mitigated | Phase 1 reads no user file paths and displays no git, author, commit, or model data. |

Blocker threshold: high. No high-severity threats are expected for this local terminal keyboard scope.
</threat_model>

## Artifacts this phase produces

- `PERSPECTIVE_HOTKEYS`
- `useKeyboard` handler in `App`
- Tab branch using `getNextPaneId`
- q branch using `process.exit(0)`
- Inert branch for hotkeys `1`, `2`, `3`, and `4`

<verification>
Before declaring plan complete:
- [ ] `bun test src/focus.test.ts` exits 0.
- [ ] `bunx tsc --noEmit` exits 0.
- [ ] Source assertions confirm `useKeyboard`, `key.name === "tab"`, `key.name === "q"`, `process.exit(0)`, and `PERSPECTIVE_HOTKEYS`.
- [ ] Manual terminal check with `bun dev` confirms three panes, Tab focus cycling, inert hotkeys 1-4, and q exit.
</verification>

<success_criteria>
- `CLI-03` is satisfied by q exiting the app cleanly.
- `TUI-05` is satisfied by Tab cycling pane focus.
- Decisions `D-05`, `D-07`, and `D-08` are represented in source and verified.
- Phase 1 still contains no git loading, author selection, analysis state, model calls, perspective labels, status line, or keymap footer.
</success_criteria>

<output>
After completion, create `.planning/phases/GT-01-opentui-shell/01-02-SUMMARY.md`.
</output>
